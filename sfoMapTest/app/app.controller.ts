/***** Class definition *****/
export class SfoBaseMapController implements ng.IComponentController {
    public static injectionName: string = 'sfoBaseMapController';

    public static $inject: string[] = [];
    constructor() { }

    // Component methods
    // Invoked after component controller is instantiated.  Use this for initialization
    public $onInit = (): void => {        
    }   
}